import { createStyles } from '@mantine/core';
import React from 'react'
type Props = {}



const ViewVisit = (props: Props) => {
    const { classes } = useStyles();

    return (
        <div>ViewVisit</div>
    )
}


const useStyles = createStyles((theme, _params, getRef) => {
    return ({

    })
});

export default ViewVisit